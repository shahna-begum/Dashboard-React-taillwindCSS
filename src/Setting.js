import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;
const Setting = () => {
    return (
        <>
            <Title level={1} className='p-11' style={{ marginBottom: 0, fontWeight: 700 }} >Setting Page</Title>
        </>
    )
}
export default Setting;